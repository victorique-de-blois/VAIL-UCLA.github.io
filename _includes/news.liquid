<div class="news">
  {% if site.news != blank %}
    {% assign news_size = site.news | size %}
    <div
      class="
        table-responsive
        {% if news_size > 3 %}
          responsive-scrollable
        {% endif %}
      "
    >
      <table class="table table-sm table-borderless" style="margin-bottom: 0;">
        {% assign news = site.news | reverse %}
        {% assign news_limit = news_size %}
        {% for item in news limit: news_limit %}
          <tr style="line-height: 1.1;">
            <th scope="row" style="width: 20%; padding-top: 0.1rem; padding-bottom: 0.1rem; padding-right: 0.3rem;">{{ item.date | date: '%b %d, %Y' }}</th>
            <td style="padding-top: 0.1rem; padding-bottom: 0.1rem; padding-left: 0.3rem;">
              {% if item.inline %}
                {{ item.content | remove: '<p>' | remove: '</p>' | emojify }}
              {% else %}
                <a class="news-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>
              {% endif %}
            </td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% else %}
    <p>No news so far...</p>
  {% endif %}
</div>